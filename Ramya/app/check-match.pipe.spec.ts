import { CheckMatchPipe } from './check-match.pipe';

describe('CheckMatchPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckMatchPipe();
    expect(pipe).toBeTruthy();
  });
});
